[project]
name = "backend"
version = "0.1.0"
description = "LiGo Backend Services"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.6",
]

[dependency-groups]
dev = [
    "django-extensions>=4.1",
    "pydotplus>=2.0.2",
    "pygraphviz>=1.14",
]
